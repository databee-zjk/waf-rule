# skill-02: 定期点検 — 何を点検するか

> 後続 AI 向けハンドブック 2/2。前提知識は `skill-01-overview.md`。
> **本ファイルは AI への指示書でもある。** 一定間隔で AI に読ませ、以下を実施させる。

## 0. なぜ点検が要るか

このプロジェクトの弱点は 1 点に集約される。
**各社の公開 IP リストは、こちらの都合と無関係に変わる。**
URL の移転・廃止、JSON 構造の変更、配布の打ち切り — いずれも予告なく起きうる。
自動化してある以上、壊れても誰も気付かないまま IPSet が古いままになる。

点検の目的は「今持っている手掛かりが、まだ機能するか」を定期的に確かめること。
異常を見つけたら **AI は勝手に直さず、何がどう変わったかを報告して人の判断を仰ぐ**。

## 1. 点検は 2 種類ある

| | 何を見るか | 頻度 | 主体 |
|---|---|---|---|
| **A. 情報源の点検** | 公開元 URL・形式が変わっていないか | 週次〜月次 | AI |
| **B. 稼働の点検** | 日次実行が失敗していないか | 日次（ログ確認） | cron + 人 |

A が「将来壊れる予兆」を見る側、B が「もう壊れている」を見る側。
どちらか片方では足りない。

---

## 2. A. 情報源の点検（AI が実施）

### 対象（6 主体）

| 主体 | 手掛かりファイル | 一次情報源 |
|---|---|---|
| Google Bot | `ip-google-bot.md` | `https://developers.google.com/static/search/apis/ipranges/googlebot.json` 他 |
| Bing Bot | `ip-bing-bot.md` | `https://www.bing.com/toolbox/bingbot.json` |
| OpenAI | `ip-openai.md` | `https://openai.com/gptbot.json` / `searchbot.json` / `chatgpt-user.json` |
| Gemini | `ip-gemini.md` | `https://developers.google.com/static/crawling/ipranges/*.json` |
| Claude | `ip-claude.md` | `https://claude.com/crawling/bots.json` 他 |
| Copilot | `ip-copilot.md` | `https://www.bing.com/toolbox/bingbot.json`（Bing 系と共通） |

URL の正は `script/get-*-ip.sh` の `SOURCES` 表。
`ip-*.md` はその根拠と出典を人が読むための資料。**両者がずれていないかも点検対象。**

### 手順

各主体について次を確認する。

1. **到達性** — 記載の URL が HTTP 200 で取得できるか。
   404 / 別ドメインへの恒久リダイレクト / タイムアウトが出ていないか。
2. **形式** — 従来どおりの JSON 構造か。
   キー名（`prefixes` / `ipv4Prefix` / `ipv6Prefix` など）が変わっていないか。
3. **件数** — CIDR 件数が前回から極端に増減していないか。
   目安は ±50% 超、または 0 件を異常とみなす。
4. **公式告知** — 公式ドキュメント／ブログに、配布 URL の移転・廃止・新設の告知が出ていないか。
   Google は 2026-03 に実際に移転した実績があるため、特に注意する。

### 判定と対応

| 結果 | 対応 |
|---|---|
| 変化なし | 結果を記録して終了。**スクリプトは触らない** |
| 変化あり | **勝手に修正しない。** 差分を明示して報告し、人の判断を得てから `script/get-*-ip.sh` の `SOURCES` と `ip-*.md` を更新する |

### 点検対象外

**AWS CLI / API 部分（`waf-ipset.sh` 側）は点検しない。**
`wafv2 list-ip-sets` / `get-ip-set` / `update-ip-set` は AWS 公式の固定仕様であり、
こちら都合で変わることはない。

---

## 3. B. 稼働の点検（日次ログで確認）

### 終了コードで判断する

| コード | 意味 | 扱い |
|---|---|---|
| 0 | 反映済み（変更あり） | 正常 |
| 3 | 変更なし（既に同内容） | 正常。日次実行なら大半はこれ |
| 1 | 取得失敗・検査不合格・書き込み失敗 | **要確認** |
| 2 | 引数・設定・入力の誤り | **要確認** |

パイプ上流（取得側）の成否は `PIPESTATUS` を見る。

### ログの見方

```bash
grep ERROR /var/log/waf-ipset.log      # 異常だけ拾う
```

行頭にスクリプト名が付くので、複数の出力が混ざっても区別できる。
aws CLI と curl の生のエラー文はそのまま残る（握り潰していない）。

### 典型的な異常と意味

| ログに出るもの | 意味 | 対応 |
|---|---|---|
| `HTTPステータスが 200 ではありません: 404` | **URL が変更または廃止された** | A の点検に移り、`SOURCES` を更新 |
| `AccessDeniedException` | IAM 権限不足 | `wafv2:ListIPSets` / `GetIPSet` / `UpdateIPSet` を付与 |
| 認証情報の期限切れ | SSO セッション切れ | `aws sso login` 等で取り直す |
| curl の通信失敗（DNS・プロキシ・証明書） | 実行環境の問題 | **cron と対話シェルは環境変数が違う。** プロキシ環境ではここで落ちる |
| 件数の急減で中止 | 上流の仕様変更で「少ないが空ではない」一覧が返った | A の点検で確認。正当なら `--MAX_SHRINK_PERCENT=100` で再実行 |

### 生成物を消さない

`script/analysis/` に実行のたび次が残る（`.gitignore` 済み）。
依頼元が後から自分で内容を確認するためのもの。**削除しないこと。**

| ファイル | 内容 |
|---|---|
| `exec-<日時>.log` | 実行記録（実行者・日時・接続先・操作内容・LockToken） |
| `update-<日時>.json` | AWS に送った JSON 全文 |

---

## 4. 報告フォーマット（A の点検）

```
点検日: YYYY-MM-DD
| 主体 | 到達性 | 形式 | 件数(前回→今回) | 判定 |
|---|---|---|---|---|
| Google Bot | OK | OK | 182 → 185 | 問題なし |
| Bing Bot   | OK | OK | 28 → 28   | 問題なし |
...
要対応: <あれば具体的に記載。なければ「なし」>
```

件数の前回値は `ip-*.md` の実測値、または前回の点検記録を基準にする。
`ip-*.md` 記載の実測値は規模の参考であり、**自動判定には使わない**（skill-01 §4 参照）。

---
[zjk 2026-09-18 AI補助]

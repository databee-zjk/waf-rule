# AWS WAF クローラIP自動化　実現可否 調査報告

作成日: 2026-09-18 ／ 対象: AWS WAF IPSet の自動更新化

---

## 1. 結論（先に3行）

1. **対象6件のうち4件は自動化できます。** Google / Bing / OpenAI / Claude は公式IPリストが公開されており、機械的に取得できます。
2. **Gemini と Copilot は「IPでの識別」ができません。** 専用のIPリストが存在しないためです。ただし**目的が「許可リスト」であれば実害はありません。** 両者は Google / Bing の基盤から通信するため、Google / Bing を許可した時点で自動的に許可されます（→ 3-2）。
3. **費用はほぼゼロです。** IP取得もAWS API呼び出しも無料。月額は WAF の基本料のみ（$5〜$10程度）。

---

## 2. 調査対象と可否一覧

| # | 対象 | 自動化 | 公開元 | 実測件数 |
|---|---|---|---|---|
| 1 | Googlebot（検索） | **可** | Google公式JSON（5ファイル） | IPv4 1,095 / IPv6 1,068 |
| 2 | Bingbot（検索） | **可** | Microsoft公式JSON | IPv4 28 |
| 3 | OpenAI | **可** | OpenAI公式JSON（3ファイル） | IPv4 279 |
| 4 | Claude | **可** | Anthropic公式JSON | IPv4 26 |
| 5 | Gemini | **不可** | — | — |
| 6 | Copilot | **不可** | — | — |

### 検証方法について
上記はドキュメントを読んだだけの推測ではなく、**全URLに実際に接続し、HTTPステータス・件数・更新日時を実測**しています。取得手順は対象ごとに線索ファイル（`ip-*.md` 全6件）として整備済みで、第三者が同じ手順で再検証できる状態です。

---

## 3. なぜ Gemini と Copilot はできないのか

両者とも**専用のIPアドレスを持っていません**。既存の検索クローラの設備を共用しているためです。

| 対象 | 実際の通信元 | 結果 |
|---|---|---|
| Gemini | Google共通のクローラ基盤 | Googlebot と**同一IP**から来る |
| Copilot | Bingbot の基盤 | Bingbot と**同一IP**から来る |

このため「IPアドレスで Gemini だけを止める」と、**Googlebot も同時に止まります**。
つまり **Google検索のインデックスから自社サイトが消えます**。Copilot と Bing の関係も同様です。

これは設定の工夫で回避できる話ではなく、相手側の構成に起因する制約です。

### 3-2. ただし目的が「許可リスト」なら、この制約は問題になりません

上記は「Gemini だけを狙って遮断する」場合の話です。
**目的が許可リスト（正規クローラを誤遮断しない）であれば、基盤を共用していることはむしろ好都合です。**
Gemini の通信元は Google のIPリストに、Copilot の通信元は Bing のIPリストに含まれるため、
**Google / Bing を許可した時点で、Gemini / Copilot も自動的に許可済みになります。**

| 対象 | 許可リストでの扱い | 追加作業 |
|---|---|---|
| Gemini | Google の IPSet に内包される | **不要** |
| Copilot | Bing の IPSet に内包される | **不要** |

ただし、この「自動的にカバーされる」には次の3点の条件が付きます。

**条件1: Google側は `common-crawlers` だけでは足りない可能性があります。**
Gemini の Deep Research 等、ユーザ操作起因の取得は `user-triggered-agents`（IPv4 12件 / IPv6 8件）に該当する可能性があります。ただし**どのUAが含まれるかをGoogle公式は明記しておらず、裏は取れていません**（未確認事項）。
件数が極小のため、許可目的であれば **`common-crawlers` + `user-triggered-agents` の2ファイルを入れる**ことを推奨します（IPv4 182件 / IPv6 155件）。取りこぼした場合の影響に対し、追加コストがほぼゼロのためです。

**条件2: Copilot のカバー率は課題①の影響を直接受けます。**
Bingbot のリストが2年8ヶ月更新されていないため、Microsoft が新しいIPを使い始めていた場合、**Copilot の通信も同じだけ取りこぼします**。対策は課題①と同一（逆引きDNSの併用）です。

**条件3: GitHub Copilot / Microsoft 365 Copilot は対象外です。**
これらは Bingbot 基盤を使わないため、Bing の許可リストではカバーされません。ただし公開Webのクロールを行わないため、本件の対象外という整理です（→ `ip-copilot.md` 4章）。

### 代替案
Gemini / Copilot の制御が必要な場合は、**WAF ではなく `robots.txt` で対応します**。
Google は `Google-Extended` というトークンを公開しており、これを `Disallow` することで
「検索インデックスには載せるが、AI学習には使わせない」という分離が可能です。
WAFでは実現できない出し分けが、robots.txt なら実現できます。

---

## 4. 実運用で想定される課題と対策

### 課題① Bingbot のリストが2年8ヶ月間、更新されていない
- **事実**: Microsoft公開JSONの更新日時が `2024-01-03` のまま（2026-09-18 実測）。
- **リスク**: Microsoft が新しいIPを使い始めても、このリストには反映されない可能性があります。許可リストとして使うと、**正規の Bingbot を誤って遮断**しかねません。
- **対策**: IPリスト単独に依存せず、逆引きDNS（`*.search.msn.com`）による検証を併用する構成とします。

### 課題② 更新の反映が「全件入れ替え」方式である
- **事実**: AWS の IPSet 更新API（`UpdateIPSet`）は差分追加ではなく、**毎回すべてを置き換える**仕様です。
- **リスク**: 取得元が一時的に落ちて空データが返った場合、そのまま同期すると**IPSetが空になります**。許可リストであれば全クローラが遮断され、検索流入が止まります。**これが本件で最も重大な事故シナリオです。**
- **対策**: 同期前に健全性チェックを必須とします。「HTTP 200 であること」「JSONとして解釈できること」「件数が前回から極端に減っていないこと」の3点を満たさない場合は**更新せず中断し、通知する**設計とします。

### 課題③ Google の公開URLが移転済み
- **事実**: 2026年3月に配置場所が変更され、旧URLは 301 応答。さらに**ファイル名も `googlebot.json` → `common-crawlers.json` に変更**されています。
- **リスク**: 古い情報を参照した実装では取得に失敗します。
- **対策**: 新URLを直接参照します（線索ファイルに新旧を併記済み）。

### 課題④ IPv4 と IPv6 を同じリストに入れられない
- **事実**: AWS の仕様上、1つの IPSet は IPv4 か IPv6 のどちらか一方のみです。
- **影響**: Google のみ IPv6 を多数保有するため、Google用は2つに分割します。他3社は IPv6 が0件のため影響ありません。
- **対策**: 設計に織り込み済み。追加コストはありません。

---

## 5. 費用について

| 項目 | 費用 |
|---|---|
| IPリストの取得（各社サイトへの接続） | **$0** |
| AWS API 呼び出し（`GetIPSet` / `UpdateIPSet`） | **$0**（課金対象外） |
| AWS WAF Web ACL | $5.00 / 月 |
| AWS WAF ルール | $1.00 / 月 × ルール数 |
| リクエスト処理 | $0.60 / 100万リクエスト |

**自動化そのものによる費用増は発生しません。** 毎時実行しても費用は変わりません。
AWS側の制限（IPSet上限1万件、更新API 毎秒1回）に対し、実際の規模は1,400件程度・1日数回のため、**十分な余裕があります**。

なお、更新頻度は対象ごとに差があります（Google は毎日更新、OpenAIの一部は当日更新、Bing は2年間変化なし）。一律の頻度ではなく、対象に応じた間隔とすることで無駄を省きます。

---

## 6. ご判断いただきたい事項

**この施策の目的は「許可」か「遮断」か**をご決定ください。方向によって設計が逆になります。

| | 許可（allow）する場合 | 遮断（block）する場合 |
|---|---|---|
| 目的 | 正規クローラがWAFに誤遮断されるのを防ぐ | AI事業者のクロールを拒否する |
| 障害時の既定動作 | 通す側に倒す | 遮断する側に倒す |
| OpenAIの扱い | 3種をまとめて可 | **3種の分割が必須**（学習のみ拒否、検索は許可等） |
| Bingの古いリスト | **リスクあり**（課題①） | 影響は軽微 |
| Gemini / Copilot | **追加作業不要**（Google / Bing に内包。→3-2） | **不可**（IPでは分離できず、robots.txt で対応） |

判断材料として、OpenAI は用途別に3つのリストを公開しており（学習用・検索用・ユーザ操作起因）、**「AI学習には使わせないが、AI検索結果には載せたい」という選択が可能**です。この点はご検討に値すると考えます。

---

## 7. スクリプトの整備状況

**自動化に必要なスクリプトは作成済みです。** 使い方は下記をご覧ください。

→ **[script/readme.ja.md](script/readme.ja.md)**（導入手順・運用方法・設定項目）

### 構成の考え方

取得と反映を分けてあります。

```
<取得スクリプト> | waf-ipset.sh replace -f - --IPSET_NAME=<名前> --apply
```

| | 役割 | 対象 |
|---|---|---|
| 取得スクリプト | 公開IPを取ってくるだけ。AWSには接続しない | Google / Bing / OpenAI / Claude の4本 |
| `waf-ipset.sh` | IPSetへ反映するだけ。取得はしない | 1本 |

**主体ごとに別々の IPSet へ反映する構成**にしてあります。
これにより第6章の「学習は拒否、AI検索は許可」といった出し分けが可能になります。
1つの IPSet にまとめると、この選択ができなくなります。

### 安全装置

課題②（全件置換による事故）への対策として、書き込み前に以下を検査します。

| 検査 | 内容 |
|---|---|
| 取得の成否 | 取得元が1つでも失敗したら、不完全なリストで上書きせず中止 |
| 空の入力 | 上流が何も出さなかった場合、書き込まずに中止 |
| 件数の急減 | 現在の登録から30%を超えて減る場合は中止（割合は変更可） |
| 既定は模擬実行 | `--apply` を付けない限り書き込まない |

また、AWS や通信でエラーが出た場合は、**元のエラー文をそのままログに残します**。
権限不足・URL変更・プロキシ不通などを、ログを見ただけで切り分けられるようにしてあります。

### 現時点の制限

| 項目 | 内容 |
|---|---|
| IPv6 | 取得はできますが、反映は未対応です。Googleが約1,000件公開しています |
| 動作確認 | **実際のAWS環境への書き込みは未実施です**（次章 参照） |

---

## 8. 次のステップ

| | 内容 | 状態 |
|---|---|---|
| 1 | 取得元の特定と検証 | **完了**（線索ファイル6件 整備済み） |
| 2 | スクリプトの作成 | **完了**（[script/readme.ja.md](script/readme.ja.md)） |
| 3 | 目的（許可／遮断）の決定 | **ご判断待ち**（第6章） |
| 4 | テスト用IPSetでの動作確認 | 環境のご用意をお願いしたい（下記） |
| 5 | 本番適用 | 3・4 の後 |

### 4 のためにご用意いただきたいもの

スクリプトは既存の IPSet を操作します（IPSet 自体の作成は行いません）。
影響のないテスト用の環境を、以下の内容でご手配いただけますでしょうか。

| 項目 | 内容 |
|---|---|
| テスト用 IPSet | **IPv4版**を1つ。名前は任意 |
| スコープ | `REGIONAL`（ALB/API Gateway）か `CLOUDFRONT` か |
| リージョン | `CLOUDFRONT` の場合は `us-east-1` 固定 |
| IAM権限 | `wafv2:ListIPSets` / `wafv2:GetIPSet` / `wafv2:UpdateIPSet` |
| 確認事項 | その IPSet が本番の Web ACL から参照されていないこと |

ご用意いただければ、取得から反映までを実環境で確認し、結果をご報告します。

---

## 付録: 関連資料

| ファイル | 内容 |
|---|---|
| **[script/readme.ja.md](script/readme.ja.md)** | **スクリプトの使い方・運用手順** |
| `ip-google-bot.md` | Googlebot の取得元・手順・実測値 |
| `ip-bing-bot.md` | Bingbot（課題①の詳細を含む） |
| `ip-openai.md` | OpenAI 3種の使い分け |
| `ip-claude.md` | Claude（IPの取り違え注意点を含む） |
| `ip-gemini.md` | IP識別不可の根拠、許可リストでのカバー範囲、robots.txt による代替案 |
| `ip-copilot.md` | IP識別不可の根拠、許可リストでのカバー範囲、各種 Copilot の区別 |

---
[zjk 2026-09-18 AI補助]
